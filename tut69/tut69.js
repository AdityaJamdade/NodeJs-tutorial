console.log("This is tut 69.0.0");

// 1(major).0(minor).0(batch)
    // a bug fixed so batch is updated
// 1.0.1

    // added a function, so a minor chanege is been made so minor is updated
// 1.1.0

    // some functions are removed inputs are changed
// 2.0.0

// to install a package 
// npm install express
// npm i express

// used if a package is needed only at the time of development and not in case of production
// npm install nodemon --save-dev

// used if we need to install a package globally on our folder or workspace
// npm install nodemon --global

// After deleting the node_modules we can run this command and install the required packages 
// npm install 